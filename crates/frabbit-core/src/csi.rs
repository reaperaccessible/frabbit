// CSI-specific code has been replaced by the manifest-driven generic
// post-install pipeline (`generic_post_install.rs`). This module is kept
// as a placeholder so `pub mod csi;` in lib.rs stays valid; it can be
// removed entirely once external crates (if any) stop referencing it.
